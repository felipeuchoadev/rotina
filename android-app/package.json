{
  "name": "redzone-android",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "sync": "cap sync android",
    "build:debug": "cd android && gradlew.bat assembleDebug",
    "install:debug": "cd android && gradlew.bat installDebug"
  },
  "dependencies": {
    "@capacitor/android": "8.5.1",
    "@capacitor/core": "8.5.1"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.1"
  }
}
